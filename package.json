{
  "name": "guavajobs",
  "private": true,
  "description": "Monorepo root. Next.js app lives in web/ (Vercel Root Directory). API lives in api/.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "lint": "npm run lint --prefix web",
    "start": "npm run start --prefix web"
  },
  "engines": {
    "node": ">=20"
  }
}
